[project]
name = "app"
version = "0.1.0"
dependencies = [ "fastapi", "uvicorn[standard]", "pymongo", "pydantic[email]", "bcrypt<4.1", "pyjwt", "python-dotenv", "pytest", "httpx" ]
requires-python = "~=3.14.0"
